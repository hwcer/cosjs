const loader = require('cosjs.loader');
const library = loader(__dirname + '/library');


exports = module.exports = function(name){
    return library.require(name);
}


exports.addPath = function(path){
    library.addPath(path);
}